# 戴冠戦の概念礼装2枚・自陣礼装重複チェック・所持鯖カタログ完全連動の実装タスク

- `[x]` **Phase 1: `app.js` のデータ連携・プロンプト強化**
  - `[x]` `buildGeminiPrompt` に所持サーヴァントの完全なカタログ (`ownedServantsInfo`) を構築して渡す（名前、クラス、特性、現在Lv、目標Lv）
  - `[x]` `templatesInfo` にスロットごとの固定礼装 (`fixedCEs`) を含めて渡す
  - `[x]` 自陣礼装の重複不可ルール（同じ礼装は1枚のみ）を指示に追加
  - `[x]` 戴冠戦 (バトル名に"戴冠戦"が含まれる) のフレンドサポート2枚装備ルールを指示に追加
    - フレンドサポートの2枚は重複不可
    - フレンドサポートの礼装は自陣の礼装と重複可能
  - `[x]` AIへの出力指示として、配分結果の構造化 JSON スキーマ（`partyCEs`、`friendCEs`、`partyBonus`）を再定義

- `[x]` **Phase 2: JS側絆ポイント計算エンジンへの完全移行**
  - `[x]` `runScheduleSimulation` 内で AI からの出力 JSON（配置枠、配置礼装、フレンド礼装、配置ボーナス）をパース
  - `[x]` 各フェーズの各スロットにおけるサーヴァントの絆ポイント (`perRunBond`) をJS側で完全に数学計算する処理の実装
  - `[x]` 各フェーズ終了時の絆Lv進捗・余剰EXPの処理、および目標達成フラグ (`completed`) の判定をJS側で実行
  - `[x]` 表示用の `ceLabels` や `completedServants` をJS側で動的に構築

- `[x]` **Phase 3: UI/UXのプレミアム化とローカル自動作成機能の追加**
  - `[x]` フェーズ表示カード (`index.html`) のデザイン拡張
  - `[x]` 各サーヴァントカードの下に、装備している概念礼装のバッジを美しく表示
  - `[x]` フレンドサポートの枠を専用セクションとして配置し、装備している1〜2枚の礼装をバッジ表示
  - `[x]` **代替案として「ローカル高速生成（ルールベース）」機能を実装！**
    - AIを使わず完全オフラインで瞬時に(0.1ms)最適な周回スケジュールを数学的にシミュレーション生成
    - 戴冠戦のフレンド2枚礼装、自陣重複禁止、特性一致優先グルーピングのルールを完璧に再現
  - `[x]` テスト周回と動作確認
